var Request = require("request");
var database = require("./helpers/database.js");

// TODO: test user routes

describe("User", () => {

  beforeAll(() => {
    // database.fill();
  });
  afterAll(() => {
    // database.dump();
  });

  // > EDITS
  // >> test make shop edit
  // >> test make snack edit
  // >> test make new shop
  // >> test make new snack

})
